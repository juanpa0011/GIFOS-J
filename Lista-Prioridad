Entender el funcionamiento de los botones "Link" (NASA)
"Maximixar" de cada elemento gifos

Crear video grabador (Funcionamiento unicamente Desktop)
    1- Framework establecido, pero no tiene conexion alguna con la camara.
    2- Seguir paso a paso las funciones necesarias.
    3- Agregar el elemento a "Mis-Gifos" usando el constructor designado.
    Extra -class="emptyScreen" debe ser display none cuando detecte al menos 1 GIFO.
    4- Clase llamada gifobuilder misgifos


Aca robe el codigo de Fede => {

        let aux = document.createElement("input");
        aux.value = https://media.giphy.com/media/${myGifoId}/giphy.gif;
        document.body.appendChild(aux);
        aux.select();
        document.execCommand("copy");
        document.body.removeChild(aux);
        alert("Link del Gif Copiado al Portapapeles");

}

Aca robe el codigo de Fede para Descargar que se la robo a Mariana (La idea)
recibe una cosa y un titulo... =>

const download = async (id, title) => {
  let a = document.createElement('a');
  try {
    let response = await fetch(https://media.giphy.com/media/${id}/giphy.gif);
    let file = await response.blob();
    a.download = title;
    a.href = window.URL.createObjectURL(file);
    a.dataset.downloadurl = ['application/octet-stream', a.download, a.href].join(':');
    a.click();
  } catch (err) {
    console.error(err);
  }
};


viewH